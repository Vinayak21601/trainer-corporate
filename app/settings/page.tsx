import { PlaceholderView } from '@/src/components/PlaceholderView';

export default function SettingsPage() {
  return (
    <PlaceholderView
      title="Settings"
      description="Manage company profile, procurement preferences, notification rules, and workspace permissions."
    />
  );
}
